# generated by AutoRust
[package]
name = "azure_svc_communicationemail"
version = "0.6.0"
edition = "2021"
license = "MIT"
description = "generated REST API bindings"
repository = "https://github.com/azure/azure-sdk-for-rust"
homepage = "https://github.com/azure/azure-sdk-for-rust"
documentation = "https://docs.rs/azure_svc_communicationemail"

[lib]
doctest = false

[dependencies]
async-trait = "0.1"
azure_core = { path = "../../../sdk/core", version = "0.5", features = [] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
bytes = "1.0"
futures = "0.3"
time = "0.3"
httpdate = "1.0.2"
hmac = "0.12.1"
sha2 = "0.10.6"
base64 = "0.13.0"
[dependencies.uuid]
version = "1.1.2"
features = [
    "v4",                # Lets you generate random UUIDs
    "fast-rng",          # Use a faster (but still sufficiently random) RNG
    "macro-diagnostics", # Enable better diagnostics for compile-time UUIDs
]

[dev-dependencies]
azure_identity = { path = "../../../sdk/identity" }
tokio = { version = "1.0", features = ["macros", "rt-multi-thread"] }
env_logger = "0.9"

[package.metadata.docs.rs]
all-features = true

[features]
default = ["package-2021-10-01-preview", "enable_reqwest"]
enable_reqwest = ["azure_core/enable_reqwest"]
enable_reqwest_rustls = ["azure_core/enable_reqwest_rustls"]
no-default-tag = []
"package-2021-10-01-preview" = []
